{% extends 'base.html.twig' %}

{% block title %}Recette{% endblock %}

{% block body %}
    <h1>Recette</h1>

    {# <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recette.id }}</td>
            </tr>
            <tr>
                <th>Description3</th>
                <td>{{ recette.description3 }}</td>
            </tr>
            <tr>
                <th>Fiche</th>
                <td>{{ recette.fiche }}</td>
            </tr>
            <tr>
                <th>Avatar</th>
                <td>{{ recette.avatar }}</td>
            </tr>
        </tbody>
    </table> #}
    <div class= "d-flex text-light">{{ recette.fiche }}</div>
       <div class= "d-flex justify-content-between">
   
                       <span>{% for categorie in recette.categories %}
                       <a href="{{ path('app_ingredient_index') }}">{{ categorie.description2 }}</a>
                       {% endfor %}
                    </span>
                
                     <span class="text-light">Préparation:120 minutes</span>
                </div>    
     <div class="card m-5 col-8"> <img src="{{ asset('images/' ~ recette.avatar2) }}" class="card-img-top" alt="..."></div>
    
 
                   
   <div class= "d-flex justify-content-center text-light"><span class="d-flex  text-light border border-primary col-8 p-5">{{ recette.description3 }}</span> 
  
   <span class="text-light">Cuisson:120 minutes</span>
   </div>
<div class="d-flex text-light justify-content-end">
<ul>
                        {% for note in recette.notes %}
                        <li class="text-light">{{ note.note }} / 5</li>
                        {% endfor %}
                    </ul></div>
<h1 class="text-light">Commentaires</h1>
<div class= "d-flex text-light"> <ul>{% for note in recette.notes %}
<li class="text-light">{{ note.description1 }}</li>

 {% endfor %}
</ul>

</div>
    <a class="btn btn-primary" href="{{ path('app_recette_index') }}" role="button">Retour vers l'accueil</a>
{% if is_granted('ROLE_MODERATOR') %}
    <a class="btn btn-info" href="{{ path('app_recette_edit', {'id': recette.id}) }}" role="button">Modifier</a>
{{ include('recette/_delete_form.html.twig', { recette: recette }) }}

    {% endif %}
{% endblock %}